astra linux special edition test:
  stage: test-rust
  tags: [alse]
  needs: ["astra linux special edition build"]
  cache:
    paths:
      - Cargo.lock
      - target/

  script:
    - cargo --version
    - cargo test