windows:
  stage: build
  tags: [windows]
  script:
    - cargo --version
    - rustup target add x86_64-pc-windows-gnuc --toolchain beta
    - /root/.cargo/bin/cross build --release --target x86_64-pc-windows-gnu
    - ls target/release -la
     
