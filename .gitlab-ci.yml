variables:
  GIT_SUBMODULE_STRATEGY: recursive
  GIT_DEPTH: 100
  PKG_VER: 0.0.0

include:
  - local: /.gitlab/alse.yml
  # Removed due to unpresent rustc package on Win64 runner
  # - local: /.gitlab/win64.yml

stages:
  - build
